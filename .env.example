# Copy this file to .env (which is gitignored) before running `docker compose up`.
# Override these defaults for local development.

# Set to 1 to run the Flask app in debug mode (auto-reload, verbose errors).
FLASK_DEBUG=0
