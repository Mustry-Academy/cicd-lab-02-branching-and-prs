# Mustry Academy — Lab 02
# A generic Flask + Redis dev stack used as the subject of branching/PR exercises.
#
# Run it:
#   cp .env.example .env
#   docker compose up -d
#
# Then probe it:
#   curl http://localhost:5050/health
#   curl "http://localhost:5050/greet?name=Ada"

services:
  app:
    build: ./sample-app
    container_name: lab02-app
    ports:
      - "5050:5050"
    environment:
      REDIS_URL: "redis://redis:6379/0"
      FLASK_DEBUG: "${FLASK_DEBUG:-0}"
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: lab02-redis
    # No published port: the app reaches redis over the compose network, and
    # publishing 6379 only invites a clash with a redis already running locally.
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
